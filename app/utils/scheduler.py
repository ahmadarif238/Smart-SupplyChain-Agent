# app/utils/scheduler.py
# Provided as a convenience if you want APScheduler later.
def start_periodic_agent():
    # For now we start agent via app.routes.agent.start_background_agent from main.py
    raise NotImplementedError("Using agent route start_background_agent; APScheduler optional.")
